<div class="card o-hidden border-0 shadow-lg my-5">
    <div class="card-body p-0">
        <!-- Nested Row within Card Body -->
        <div class="row">
            <div class="col-lg-5 d-none d-lg-block bg-register-image"></div>
            <div class="col-lg-7">
                <div class="p-5">
                    <div class="text-center">
                        <h1 class="h4 text-gray-900 mb-4">Kullanici Oluşturun</h1>
                        <% if (locals.validation_error != undefined) { %>
                           <%- include('./partials/_mesaj') %> 
                        <% } %>
                    </div>
                    <form class="user" action="/register" method="post">
                        <div class="form-group row">
                            <div class="col-sm-6 mb-3 mb-sm-0">
                                <input type="text" value="<%= typeof ad != 'undefined' ? ad : '' %>" name="ad" class="form-control form-control-user" id="exampleFirstName"
                                    placeholder="Adiniz">
                            </div>
                            <div class="col-sm-6">
                                <input type="text" value="<%= typeof soyad != 'undefined' ? soyad : '' %>" name="soyad" class="form-control form-control-user" id="exampleLastName"
                                    placeholder="Soyadiniz">
                            </div>
                        </div>
                        <div class="form-group">
                            <input type="email" value="<%= typeof email != 'undefined' ? email : '' %>" name="email" class="form-control form-control-user" id="exampleInputEmail"
                                placeholder="Emailiniz">
                        </div>
                        <div class="form-group row">
                            <div class="col-sm-6 mb-3 mb-sm-0">
                                <input type="password" value="<%= typeof sifre != 'undefined' ? sifre : '' %>" name="sifre" class="form-control form-control-user"
                                    id="exampleInputPassword" placeholder="Şifreniz">
                            </div>
                            <div class="col-sm-6">
                                <input type="password" value="<%= typeof resifre != 'undefined' ? resifre : '' %>" name="resifre" class="form-control form-control-user"
                                    id="exampleRepeatPassword" placeholder="Şifrenizi Tekrar Yazin">
                            </div>
                        </div>
                        <button type="submit" class="btn btn-primary btn-user btn-block">
                            Kullanici Oluştur
                        </button>
                        <hr>
                    </form>
                    <hr>
                    <div class="text-center">
                        <a class="small" href="/forget-password">Şifremi Unuttum</a>
                    </div>
                    <div class="text-center">
                        <a class="small" href="/login">Hesabiniz Var Mi? Giriş Yap!</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


<% 
const aktif = aktifSayfa;
const onceki = aktif - 1;
const sonraki = parseInt(aktif) + 1;
const toplamSayfaSayisi = sayfaSayisi;

%> 


<% if (toplamSayfaSayisi > 1) { %>
    <nav class="pagination is-medium is-centered" role="navigation" aria-label="pagination">
        <a <%= aktif != 1 ? 'href=/yonetim?page=' + onceki : '' %>  class="pagination-previous" <%= aktifSayfa ==1 ? 'disabled' : '' %>  >Önceki</a>
        <a <%= aktif != toplamSayfaSayisi ? 'href=/yonetim?page=' + sonraki : '' %> class="pagination-next" <%= aktif == toplamSayfaSayisi ? 'disabled' : ''%> >Sonraki</a>
        <ul class="pagination-list">
    
    
            <% for( let index = 1; index <= sayfaSayisi; index++ ) { %>
                <li>
                  <a href="/yonetim?page=<%=index%>" class="pagination-link <%= index == aktifSayfa ? 'is-current' : '' %> " aria-label="Goto page "><%- index %> </a>
                </li>
            <% } %>
        </ul>
      </nav>
<% } %>